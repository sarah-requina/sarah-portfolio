import { NextResponse } from "next/server";

// This route is a placeholder — replace /public/sarah-portrait.jpg with the real photo
export function GET() {
  return new NextResponse(null, { status: 204 });
}
